/*
  Header file for partition class.
*/
#ifndef PARTITION_H
#define PARTITION_H

// Need these? FIXME
#include "structs.h"
using Eigen::VectorXf;

class Partition {
  private:
    vector<Component> partition;
    masterProblem masterProb;
    Subproblem subProb;
  public:

}
